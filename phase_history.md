# StockBallDB — Phase History

## Phase 0 — Project Foundation

**Status:** COMPLETE

- Planned and implemented greenfield Python foundation under `src/stockballdb` (config, logging, SQLAlchemy DB layer, `check_db`).
- Added `requirements.txt`, editable `pyproject.toml`, Alembic scaffold (no schema migrations), pytest, `.gitignore`, `.env.example`, and README.
- Registered Tech stack and workflow in `docs/StockBallDB_index.md`.
- Initial verification: imports and unit tests passed; live PostgreSQL check blocked until `.env` existed.
- After local `.env` was configured: `check_db` SUCCESS, Alembic online, pytest 6/6 (DB integration test updated to load dotenv).

### End-of-phase summary

**Done:** Reproducible Phase 0 foundation — package layout, env-driven config, console logging, PostgreSQL connectivity (`SELECT 1`), Alembic ready for Phase 1, pytest suite, and developer README.
**Problems:** Connection verification was blocked until `DATABASE_URL` was set in `.env`; the DB integration test initially skipped because it did not call `load_dotenv()` before reading the environment.
**Remember:** Always load dotenv in integration tests that need `.env`; never commit secrets; keep Phase 0 free of canonical tables — schema migrations belong to Phase 1; prefer simple `requirements.txt` + editable install over heavier packaging tools unless the repo already uses them.

## Phase 1 — trading_days

**Status:** COMPLETE

- Locked Phase 1A design: pinned `pandas_market_calendars==5.4.0` NYSE authority; ISO `week_of_year` / weekday 1–5; NULL prev/next edges; `trading_days` only.
- Added SQLAlchemy model, Alembic migration `797f9229f6a2_create_trading_days`, calendar generate/derive/upsert/validate modules, and `python -m stockballdb.build_trading_days`.
- Built and validated **17,531** rows from 1957-01-02 through 2026-08-27; pytest 13/13 passed.
- Updated schema, definitions, sources, workflow, and README to match the locked design.

### End-of-phase summary

**Done:** Canonical `trading_days` table, deterministic NYSE spine generation (1957→today ET), idempotent upsert (with out-of-range delete), structural + historical spot-check validation, and docs aligned to Phase 1 scope.
**Problems:** Partial-range upsert initially left newer DB rows (duplicate NULL `next_trading_date`); fixed by deleting dates outside `[start, end]` before upsert. Autogenerated migration wired successfully once `target_metadata` pointed at models.
**Remember:** Pin calendar library versions; treat upgrades as reviewed events; do not put holiday/early-close research flags on `trading_days` (`calendar_context` later); batch PostgreSQL upserts to stay under parameter limits; Phase 2 is the remaining six tables — not another calendar rebuild.

## Phase 2A — daily_market_data

**Status:** COMPLETE

- Added `DailyMarketData` model + Alembic migration `555248ed3eef` (FK to `trading_days`; derived columns nullable).
- Implemented Tiingo fetch/normalize/validate/upsert and `python -m stockballdb.build_daily_market_data` for the 14 ETFs.
- Loaded **89,404** rows (1993-01-29 → 2026-08-26); idempotent rebuild confirmed; pytest 19/19.
- Documented verified Tiingo field names; left derived fields and zero-vs-NULL interpretation as TBD.

### End-of-phase summary

**Done:** Observed Tiingo pipeline for 14 ETFs — raw + adjusted OHLCV, `divCash`/`splitFactor` stored as provider values, date-aligned to `trading_days`, idempotent upsert, tests, and docs.
**Problems:** Tiingo without `startDate` returns only the latest bar (fixed with `startDate=1957-01-01`); Windows console crashed on Unicode arrow in CLI output (replaced with ASCII `->`).
**Remember:** Do not invent raw/adjusted basis for derived fields; store Tiingo corp-action zeros/ones as observed; never fabricate pre-inception rows; next tables are separate phases — stop after 2A.

## Phase 2B — Derived market-data definition investigation

**Status:** COMPLETE

- Compared raw vs adjusted formulas on live `daily_market_data` (1,365 dividend rows; 7 split rows).
- Locked ADJUSTED for `return_1d`, `gap_pct`, `drawdown_from_high`; RAW for `intraday_return` and `range_pct`.
- Locked corp-action interpretation (`0.0` / `1.0` / `NULL`) without rewriting stored observations.
- Implemented `derive_daily_market_fields` + `python -m stockballdb.derive_daily_market_data`; populated 89,404 rows (14 NULL `return_1d`/`gap_pct` = first row per symbol); pytest 23/23.

### End-of-phase summary

**Done:** Locked Phase 2B definitions in docs; deterministic provider-independent derivation; DB populated and validated (QQQ split + SPY dividend cases); observed OHLCV preserved.
**Problems:** None blocking; Windows PowerShell quoting made ad-hoc SQL awkward (used scripts instead).
**Remember:** ADJUSTED derived fields are retrospectively normalized economic history, not PIT quotes; do not rewrite Tiingo `0.0`/`1.0` to NULL; next table work is a separate phase.

## Phase 2C — market_outcomes

**Status:** COMPLETE

- Investigation locked: adjusted close→close forward returns; `t+N` = Nth subsequent same-symbol `daily_market_data` observation; signed max-up/max-down over `t+1..t+N`; full-horizon NULL; positive flags with zero → false; look-ahead documented (retrospective labels only).
- Added `MarketOutcome` model + Alembic migration `b8ed4f8d4839_create_market_outcomes`.
- Implemented derive/upsert/validate + `python -m stockballdb.build_market_outcomes` (no Tiingo calls; source = `daily_market_data` only).
- Built **89,404** rows (14 symbols; 1993-01-29 → 2026-08-26); horizon NULLs match 14/42/70/140/280 pattern; idempotent rebuild; `daily_market_data` fingerprint unchanged; pytest 27/27.

### End-of-phase summary

**Done:** Canonical `market_outcomes` table with locked formulas, deterministic per-symbol derivation, full-horizon NULL behavior, DB validation (including `return_1d(t) ≈ daily_market_data.return_1d(t+1)`), tests, and docs.
**Problems:** None blocking.
**Remember:** Outcomes are retrospective labels — never treat as point-in-time inputs on `date`; do not mutate frozen `daily_market_data`; stop here — do not begin `asset_regimes` or other tables in this phase.

## Phase 2D — asset_regimes

**Status:** COMPLETE

- Investigation locked: adjusted trailing returns/SMAs; annualized `volatility_20d` (`ddof=1`, √252); `drawdown_pct` mirrors `drawdown_from_high`; semantic trend/momentum/expanding-vol regimes; PIT through `t` only.
- Added `AssetRegime` model + Alembic migration `c3a91f0e2b7d_create_asset_regimes`.
- Implemented derive/upsert/validate + `python -m stockballdb.build_asset_regimes` (no API / no `market_outcomes`).
- Built **89,404** rows (14 symbols; exact 1:1 with `daily_market_data`); NULL boundaries match 5/20/60 and SMA/vol/regime rules; drawdown identity 0 mismatches; pytest 36/36.

### End-of-phase summary

**Done:** Canonical `asset_regimes` with locked formulas, deterministic per-symbol PIT derivation, expanding vol-regime anti-leakage checks, docs, CLI, and tests.
**Problems:** None blocking.
**Remember:** Do not tune regimes against `market_outcomes`; adjusted series remain retrospectively normalized; stop here — do not begin `macro_conditions` or other tables.

## Phase 2E — macro_conditions definition investigation

**Status:** INVESTIGATION COMPLETE — awaiting definition/source lock (no table created)

- Queried live FRED/ALFRED for candidate series (CPI, UNRATE, ICSA, DFF, DGS2/10, WALCL, BAA10Y, ICE OAS, NAPM).
- Confirmed material revisions for CPI/UNRATE/ICSA; market yields/EFFR largely stable across vintages in sampled windows.
- Recommended series map, PIT/availability rules, DEFER PMI (ISM removed from FRED; not freely reproducible), credit spread = BAA10Y, derive yield curve; schema CHANGE to drop `pmi` from V1.
- Stale notification: first FRED probe timed out; superseded by successful lean API pull already used in the investigation.

## Phase 2E — macro_conditions

**Status:** COMPLETE

- Locked series map + modified inflation_regime (distinct PIT releases, not daily duplicates); `pmi` omitted from V1.
- Added `MacroCondition` model + Alembic `d4b72c1a9e05`; FRED/ALFRED fetch, PIT align, forward-fill, derive curve/regimes; CLI `python -m stockballdb.build_macro_conditions`.
- Built **17,531** rows (1:1 `trading_days`); pytest 47/47; existing market tables preserved.

### End-of-phase summary

**Done:** Canonical `macro_conditions` with locked FRED/ALFRED sources, PIT vintages for CPI/UNRATE/ICSA, release alignment, approved fills, regimes, docs, tests.
**Problems:** ICSA ALFRED depth sparse before ~2009 (NULL/best-effort); CPI/core earliest limited by vintage availability (~1972 / ~1996); float noise on yield curve rounded to 8 dp.
**Remember:** Do not invent PMI; inflation_regime must use distinct releases; stop before `scheduled_events`.

## Phase 2F — scheduled_events definition investigation

**Status:** COMPLETE — definitions locked and implemented in Phase 2F build

- Reviewed manifesto/schema/definitions/sources/workflow, frozen macro pipeline, and the conceptual event-field draft vs current docs draft.
- Verified Fed historical FOMC pages (incl. 1957 meetings; 2008/2020 unscheduled labels), FRED/ALFRED CPI & Employment Situation release dates, BLS schedule pages, statutory election-day rule, and earnings/consensus feasibility.
- Recommended V1 scope: scheduled FOMC decision days, CPI releases, Employment Situation releases, U.S. presidential + midterm Election Days; DEFER earnings, consensus/surprise numerics, unscheduled Fed actions, and `event_window`.

## Phase 2F — scheduled_events

**Status:** COMPLETE

- Locked occurrence-calendar schema (no actual/expected/surprise/event_window; no `event_date` FK to `trading_days`); election `release_session=unknown`.
- Added `ScheduledEvent` model + Alembic `e5c83b2d4a16`; Fed HTML + ALFRED first-print + statutory election loaders; CLI `python -m stockballdb.build_scheduled_events`.
- Built **2,613** rows (fomc 683, cpi 954, employment_situation 942, election 34); idempotent rebuild; frozen tables preserved; pytest 58/58.

### End-of-phase summary

**Done:** Canonical `scheduled_events` occurrence calendar with deterministic `event_id`, scheduled-only FOMC, CPI/Employment Situation first-print releases, presidential/midterm elections, docs, tests, CLI.
**Problems:** Fed year pages 2021+ return 404 (used `fomccalendars.htm`); ALFRED first-print CPI/employment coverage starts ~1972/~1960 rather than 1957; pre-modern event times intentionally unknown.
**Remember:** Occurrence ≠ advance-schedule PIT knowledge; do not store macro values or surprises here; `event_window` belongs in `calendar_context`; stop before next phase.

## Phase 2G — calendar_context definition investigation

**Status:** COMPLETE — definitions locked and implemented in Phase 2G build

- Inspected draft calendar_context fields against Phase 1 `trading_days` (ISO week, month/quarter/year ends) and Phase 2F `scheduled_events`.
- Verified NYSE early closes (481), adhoc closures including 9/11 and Sandy, Good Friday/Thanksgiving gaps, week-length distribution, and off-calendar events (elections through 1980; some CPI/FOMC/employment).
- Recommended compact V1 schema: holiday adjacency + shortened session/week + narrow turn/quarter/year transitions + event-day and days_since fields; DEFER forward `days_to_next_*`, tax/payday/earnings/period windows.

## Phase 2G — calendar_context

**Status:** COMPLETE

- Locked schema (no forward distances / tax / payday / election-period); model + Alembic `f6d94c3e5b27`.
- Derived **17,531** rows 1:1 with `trading_days` from spine + pinned NYSE early_closes/holidays + `scheduled_events`; CLI `build_calendar_context`.
- Validated holiday geometry, ISO weeks, transitions, event flags, off-calendar `days_since` anchoring; idempotent rebuild; frozen tables preserved; pytest 71/71.

### End-of-phase summary

**Done:** Final original V1 canonical table `calendar_context` with holiday/session/week/transition geometry and retrospective event-day + `days_since_last_*` context.
**Problems:** Pandas `None`→`nan` required explicit NULL coercion on upsert; CPI/employment event row counts exceed distinct on-calendar dates when ALFRED first-prints share dates.
**Remember:** All seven V1 tables complete; do not invent forward schedule knowledge; ISO week uses `isocalendar()` year+week; stop — no post-V1 expansion in this phase.

## Phase 3A — V1 Integration & Reproduction Investigation

**Status:** COMPLETE — investigation approved; implemented in Phase 3B

- Audited eight build/derive CLIs, `check_db`, Alembic chain through `f6d94c3e5b27`, upsert vs full-replace patterns, and credential requirements.
- Verified dependency DAG (trading_days spine; market branch; macro/events branches; calendar_context last) and recommended `python -m stockballdb.build_v1` + separate `validate_v1`.
- Defined empty-DB contract, fail-fast partial-build policy, structural vs current-source vs snapshot reproducibility, and V1 freeze/versioning recommendations.

### End-of-phase summary

**Done:** Approved integration design for `build_v1` / `validate_v1`, locked build order including `derive_daily_market_data`, empty-DB contract, fail-fast/PARTIAL policy, and reproducibility language.
**Problems:** None material — design only.
**Remember:** Do not stamp 1.0.0 until fresh-DB proof; never wipe the working database; orchestrate existing builders.

## Phase 3B — V1 Integration & Reproduction

**Status:** COMPLETE

- Implemented `stockballdb/v1/{preflight,stages,report}.py`, `build_v1`, and `validate_v1` (fail-fast sequential orchestration; no schema/universe changes; version remains `0.0.1`).
- Fresh-DB proof on `stockballdb_v1_rebuild`: Run1 + Run2 both `V1 READY`; identical grains/fingerprints; working `stockballdb` untouched.
- Docs: workflow §12 reproduction + §18 reproducibility contract; concise README; `build_reports/` gitignored; pytest **84/84**.
- Background Run1/Run2/`validate_v1` shell jobs confirmed success (no new work).

### End-of-phase summary

**Done:** End-to-end V1 reproduction command + whole-DB validator + acceptance proof on a separate empty PostgreSQL database; integration tests; documentation.
**Problems:** Console stage lines can buffer when stdout is not a TTY (fixed with flush); live providers may advance calendar/max dates between distant rebuilds.
**Remember:** Structural + current-source rebuild supported; exact snapshot NOT guaranteed; do not bump to 1.0.0 or tag without explicit user approval after this report.

## StockBallDB V1.0.0 — V1 freeze milestone

**Status:** STAMPED

Accepted after all seven canonical tables completed; fresh empty-database reproduction on `stockballdb_v1_rebuild` succeeded; `validate_v1` passed; second full `build_v1` proved idempotency; pytest **84/84**; original working `stockballdb` remained untouched.

**V1 reproducibility contract:**

- Structural reproducibility: **YES**
- Current-source rebuild: **YES**
- Exact historical snapshot reproducibility: **NO** (not byte-for-byte)

Package/project version stamped **1.0.0**; annotated Git tag `v1.0.0` on commit `700424c`; pushed to GitHub (`origin/main` + tag `v1.0.0`).
